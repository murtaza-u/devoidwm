#ifndef TAGS_H
#define TAGS_H

#include "client.h"
#include "key.h"

bool getfullscrlock(unsigned int);
void view(Arg arg);
void toggletag(Arg arg);
void sendclient(Arg arg);
void tag(Arg arg);

#endif
